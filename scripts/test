#!/bin/bash

pushd . > /dev/null

cd "${0%/*}"
cd ..

echo "=== Assembling ==="
cd compiler
sbcl --script script.lisp "../programs/$1.hxa"
cd ..

echo "=== Running ==="
./bin/vm -s -p -f "programs/$1.hxb"

echo "=== Finished ==="

popd > /dev/null
